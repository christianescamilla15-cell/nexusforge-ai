{
  "name": "Validate TypeScript types",
  "comment": "Check types when a TS/TSX file is edited",
  "when": {
    "type": "fileEdited",
    "pattern": "**/*.{ts,tsx}"
  },
  "then": {
    "type": "askAgent",
    "prompt": "A TypeScript file was edited. Run type checking (tsc --noEmit) and report any type errors."
  }
}