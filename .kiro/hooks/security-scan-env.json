{
  "name": "Security scan on env change",
  "comment": "Alert when .env files are modified",
  "when": {
    "type": "fileEdited",
    "pattern": "**/.env*"
  },
  "then": {
    "type": "askAgent",
    "prompt": "An .env file was modified. Check that no secrets are being committed and .gitignore includes this file."
  }
}