{
  "name": "Security scan on env change",
  "comment": "Alert when environment or config files are modified",
  "when": {
    "type": "fileEdited",
    "pattern": "**/.env*"
  },
  "then": {
    "type": "alert",
    "message": "Environment file modified. Verify no secrets are exposed and .gitignore is correct."
  }
}